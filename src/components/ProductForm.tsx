import React from 'react';
import { Product } from '../types/product';

export default function ProductForm({ initial }: { initial?: Partial<Product> }) {
  return (
    <form>
      <p>Product form placeholder</p>
    </form>
  );
}
